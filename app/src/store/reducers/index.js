export const initialState = {
    lookingForGoat: false,
    oopsie: "",
    goat: null
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}